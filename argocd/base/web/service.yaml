apiVersion: v1
kind: Service
metadata:
  name: aegisx-web
  labels:
    app: aegisx-web
    app.kubernetes.io/name: aegisx-web
    app.kubernetes.io/part-of: aegisx-platform
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: aegisx-web
    app.kubernetes.io/name: aegisx-web
    app.kubernetes.io/part-of: aegisx-platform
