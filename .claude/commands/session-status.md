# Session Status

แสดงสถานะ session ปัจจุบันและ token usage

## Instructions

1. อ่านไฟล์ทั้งหมดใน `docs/sessions/.current/*.log`

2. สำหรับแต่ละ session แสดง:

   ```
   📋 Active Sessions

   ┌─────────────────────────────────────────────┐
   │ Session: {NAME}                             │
   │ Started: {TIME}                             │
   │ Model: {MODEL}                              │
   ├─────────────────────────────────────────────┤
   │ Tasks: {DONE}/{TOTAL}                       │
   │ Tokens: {SUM}                               │
   │ Cost: ${COST}                               │
   ├─────────────────────────────────────────────┤
   │ Completed:                                  │
   │ ✅ 1.1: description (45k)                   │
   │ ✅ 1.2: description (38k)                   │
   │ Remaining:                                  │
   │ ⬜ 1.3: description                         │
   └─────────────────────────────────────────────┘
   ```

3. แสดง total across all sessions:

   ```
   📊 Today's Total
   ├── Sessions: {X}
   ├── Tasks: {Y}
   ├── Tokens: {Z}
   └── Cost: ${W}
   ```

4. ถ้าไม่มี active sessions:
   ```
   ℹ️ No active sessions
   Use /start-session to begin
   ```

## Quick Check

- `/session-status` - ดูทุก sessions
- `/cost` - ดู Claude Code cost (built-in)
- `/context` - ดู token usage (built-in)
