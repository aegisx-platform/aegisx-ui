import type { Knex } from 'knex';

/**
 * Migration: Add budget requests permissions
 *
 * PLACEHOLDER: This migration was previously run but the file was deleted.
 * Created as placeholder to satisfy Knex migration validation.
 * The actual changes were already applied to the database.
 */

export async function up(knex: Knex): Promise<void> {
  // Placeholder - migration already applied
  console.log('Budget requests permissions already applied');
}

export async function down(knex: Knex): Promise<void> {
  // Placeholder - no rollback needed
  console.log('Budget requests permissions rollback skipped (placeholder)');
}
