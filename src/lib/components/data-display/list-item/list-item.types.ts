/**
 * Type definitions for ax-list-item.
 *
 * A compact 3-line row used in master-detail list panes, hub cards,
 * and any "code + title + meta" presentation.
 */

/** Layout density — tightens vertical padding on `compact`. */
export type ListItemDensity = 'comfortable' | 'compact';
